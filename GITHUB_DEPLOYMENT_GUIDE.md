# دليل ربط التطبيق مع GitHub وتنزيل الكود

## 📋 المحتويات
1. [إنشاء حساب GitHub](#1-إنشاء-حساب-github)
2. [إنشاء مستودع جديد](#2-إنشاء-مستودع-جديد)
3. [تنزيل الكود من الموقع](#3-تنزيل-الكود-من-الموقع)
4. [رفع الكود إلى GitHub](#4-رفع-الكود-إلى-github)
5. [نشر التطبيق](#5-نشر-التطبيق)

---

## 1. إنشاء حساب GitHub

إذا لم يكن لديك حساب على GitHub:

1. اذهب إلى [github.com](https://github.com)
2. انقر على **Sign up** (التسجيل)
3. أدخل بريدك الإلكتروني
4. أنشئ كلمة مرور قوية
5. اختر اسم مستخدم
6. أكمل عملية التحقق

---

## 2. إنشاء مستودع جديد (Repository)

بعد تسجيل الدخول إلى GitHub:

1. انقر على أيقونة **+** في الزاوية العلوية اليمنى
2. اختر **New repository** (مستودع جديد)
3. املأ المعلومات:
   - **Repository name**: `samco-ai-app` (أو أي اسم تريده)
   - **Description**: "تطبيق سامكو لصناعة المحتوى بالذكاء الاصطناعي"
   - اختر **Public** (عام) أو **Private** (خاص)
   - **لا تضف** README أو .gitignore أو license (سنرفع الكود الموجود)
4. انقر على **Create repository** (إنشاء المستودع)

---

## 3. تنزيل الكود من الموقع

### الطريقة الأولى: تنزيل ملف ZIP

إذا كان الموقع يوفر زر تنزيل:

1. ابحث عن زر **Download** أو **تنزيل** في واجهة الموقع
2. انقر عليه لتنزيل ملف ZIP يحتوي على كل الكود
3. فك ضغط الملف في مجلد على جهازك

### الطريقة الثانية: نسخ الملفات يدوياً

إذا لم يكن هناك زر تنزيل:

1. أنشئ مجلد جديد على جهازك باسم `samco-ai-app`
2. انسخ جميع الملفات والمجلدات من الموقع إلى هذا المجلد
3. تأكد من نسخ:
   - مجلد `src/`
   - مجلد `public/`
   - ملفات `package.json`, `vite.config.ts`, `tsconfig.json`
   - جميع ملفات الإعدادات الأخرى

---

## 4. رفع الكود إلى GitHub

### المتطلبات:
- تثبيت Git على جهازك ([تنزيل Git](https://git-scm.com/downloads))

### الخطوات:

#### أ. افتح Terminal أو Command Prompt

**في Windows:**
- اضغط `Win + R`
- اكتب `cmd` واضغط Enter

**في Mac/Linux:**
- افتح Terminal من Applications

#### ب. انتقل إلى مجلد المشروع

```bash
cd path/to/samco-ai-app
```

مثال:
```bash
cd C:\Users\YourName\Desktop\samco-ai-app
```

#### ج. تهيئة Git في المجلد

```bash
git init
```

#### د. إضافة جميع الملفات

```bash
git add .
```

#### هـ. إنشاء أول Commit

```bash
git commit -m "Initial commit: Samco AI App"
```

#### و. ربط المجلد بمستودع GitHub

استبدل `YOUR_USERNAME` و `YOUR_REPO_NAME` بمعلوماتك:

```bash
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
```

مثال:
```bash
git remote add origin https://github.com/samco-designer/samco-ai-app.git
```

#### ز. رفع الكود إلى GitHub

```bash
git branch -M main
git push -u origin main
```

**ملاحظة:** قد يطلب منك إدخال اسم المستخدم وكلمة المرور الخاصة بـ GitHub.

---

## 5. نشر التطبيق

بعد رفع الكود إلى GitHub، يمكنك نشر التطبيق باستخدام إحدى هذه الخدمات:

### أ. Vercel (موصى به)

1. اذهب إلى [vercel.com](https://vercel.com)
2. سجل الدخول باستخدام حساب GitHub
3. انقر على **New Project**
4. اختر مستودع `samco-ai-app`
5. اترك الإعدادات الافتراضية (Vercel سيكتشف أنه مشروع Vite تلقائياً)
6. انقر على **Deploy**
7. انتظر بضع دقائق حتى يكتمل النشر
8. ستحصل على رابط مباشر للتطبيق!

### ب. Netlify

1. اذهب إلى [netlify.com](https://netlify.com)
2. سجل الدخول باستخدام حساب GitHub
3. انقر على **Add new site** > **Import an existing project**
4. اختر **GitHub** وحدد مستودع `samco-ai-app`
5. إعدادات البناء:
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
6. انقر على **Deploy site**
7. انتظر حتى يكتمل النشر

### ج. GitHub Pages

1. في مستودع GitHub، اذهب إلى **Settings**
2. في القائمة الجانبية، انقر على **Pages**
3. في **Source**، اختر **GitHub Actions**
4. أنشئ ملف `.github/workflows/deploy.yml` في مستودعك:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install pnpm
        run: npm install -g pnpm
          
      - name: Install dependencies
        run: pnpm install
        
      - name: Build
        run: pnpm run build
        
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

5. احفظ الملف وارفعه إلى GitHub
6. سيتم نشر التطبيق تلقائياً على `https://YOUR_USERNAME.github.io/YOUR_REPO_NAME`

---

## 📝 ملاحظات مهمة

### متغيرات البيئة (Environment Variables)

إذا كان التطبيق يستخدم متغيرات بيئة (مثل API Keys)، تأكد من:

1. **عدم رفع** ملف `.env` إلى GitHub (يجب أن يكون في `.gitignore`)
2. إضافة المتغيرات في إعدادات منصة النشر:
   - **Vercel**: Settings > Environment Variables
   - **Netlify**: Site settings > Environment variables
   - **GitHub Pages**: Settings > Secrets and variables > Actions

### تحديث الكود

عندما تريد تحديث الكود:

```bash
# 1. تأكد من أنك في مجلد المشروع
cd path/to/samco-ai-app

# 2. أضف التغييرات
git add .

# 3. أنشئ commit جديد
git commit -m "وصف التحديث"

# 4. ارفع التحديثات
git push origin main
```

سيتم نشر التحديثات تلقائياً إذا كنت تستخدم Vercel أو Netlify!

---

## 🆘 حل المشاكل الشائعة

### مشكلة: "git: command not found"
**الحل:** قم بتثبيت Git من [git-scm.com](https://git-scm.com/downloads)

### مشكلة: "Permission denied"
**الحل:** استخدم Personal Access Token بدلاً من كلمة المرور:
1. اذهب إلى GitHub Settings > Developer settings > Personal access tokens
2. أنشئ token جديد
3. استخدمه بدلاً من كلمة المرور عند الرفع

### مشكلة: "Build failed" عند النشر
**الحل:** 
1. تأكد من أن جميع الملفات موجودة
2. تحقق من أن `package.json` يحتوي على جميع التبعيات
3. راجع سجلات الأخطاء (logs) في منصة النشر

---

## 📚 موارد إضافية

- [دليل Git الرسمي](https://git-scm.com/doc)
- [دليل GitHub](https://docs.github.com)
- [دليل Vercel](https://vercel.com/docs)
- [دليل Netlify](https://docs.netlify.com)

---

## 🎉 تهانينا!

الآن لديك تطبيق سامكو منشور على الإنترنت ومتصل بـ GitHub! 🚀

يمكنك مشاركة الرابط مع أي شخص ليستخدم التطبيق.

---

**© 2025 سامكو لصناعة المحتوى Ai**
