# ميزة تحويل النص إلى صوت (Text-to-Speech)

## نظرة عامة

تم إضافة ميزة تحويل النص إلى صوت (Text-to-Speech - TTS) إلى أداة إنشاء الفيديو، مما يتيح للمستخدمين توليد صوت تلقائيًا من النص المكتوب باللغتين العربية والإنجليزية.

---

## الميزات الرئيسية

### 1. توليد صوت تلقائي من النص
- **تحويل فوري**: تحويل أي نص مكتوب إلى صوت بضغطة زر واحدة
- **دعم لغتين**: العربية والإنجليزية مع أصوات طبيعية
- **جودة عالية**: استخدام Web Speech API المدمج في المتصفح
- **بدون تكلفة**: لا يتطلب اشتراكات أو خدمات خارجية

### 2. معاينة الصوت
- **مشغل صوتي مدمج**: معاينة الصوت المولد قبل استخدامه
- **تحكم كامل**: تشغيل، إيقاف، تقديم، وتأخير
- **واجهة واضحة**: عرض حالة الصوت المولد بشكل مرئي

### 3. التكامل السلس
- **حفظ تلقائي**: الصوت المولد يُحفظ كملف صوتي تلقائيًا
- **استخدام مباشر**: يمكن استخدام الصوت المولد مباشرة في الفيديو
- **تنسيق WebM**: صيغة صوتية حديثة ومدعومة على نطاق واسع

---

## كيفية الاستخدام

### الخطوات الأساسية:

1. **تفعيل الصوت**
   - اضغط على زر "إضافة صوت للفيديو" لتفعيل القسم الصوتي

2. **اختيار اللغة**
   - اختر اللغة المناسبة (🇸🇦 العربية أو 🇺🇸 English)

3. **كتابة النص**
   - أدخل النص الذي تريد تحويله إلى صوت (حتى 1000 حرف)
   - النص يمكن أن يكون أي محتوى: وصف، حوار، تعليق صوتي، إلخ

4. **توليد الصوت**
   - اضغط على زر "🎙️ توليد صوت من النص (Text-to-Speech)"
   - انتظر بضع ثوانٍ حتى يتم توليد الصوت

5. **معاينة الصوت**
   - استمع إلى الصوت المولد باستخدام المشغل الصوتي
   - تأكد من جودة الصوت ووضوحه

6. **استخدام في الفيديو**
   - الصوت المولد جاهز للاستخدام في الفيديو
   - اضغط "توليد الفيديو" لإنشاء الفيديو مع الصوت

---

## التقنيات المستخدمة

### Web Speech API
```typescript
// استخدام Speech Synthesis API
const utterance = new SpeechSynthesisUtterance(voiceText);
utterance.lang = voiceLanguage === 'ar' ? 'ar-SA' : 'en-US';
utterance.rate = 1.0;  // سرعة الصوت
utterance.pitch = 1.0; // نبرة الصوت
utterance.volume = 1.0; // مستوى الصوت

window.speechSynthesis.speak(utterance);
```

### Audio Recording
```typescript
// تسجيل الصوت المولد
const audioContext = new AudioContext();
const destination = audioContext.createMediaStreamDestination();
const mediaRecorder = new MediaRecorder(destination.stream);

mediaRecorder.ondataavailable = (event) => {
  audioChunks.push(event.data);
};

mediaRecorder.onstop = () => {
  const audioBlob = new Blob(audioChunks, { type: 'audio/webm' });
  const audioFile = new File([audioBlob], `tts_${lang}_${Date.now()}.webm`);
};
```

---

## الحالات والمتغيرات

### State Management
```typescript
// حالات TTS
const [isGeneratingTTS, setIsGeneratingTTS] = useState(false);
const [generatedAudioUrl, setGeneratedAudioUrl] = useState<string | null>(null);
const audioPreviewRef = useRef<HTMLAudioElement>(null);
```

### Cleanup
```typescript
// تنظيف URL عند إلغاء التحميل
useEffect(() => {
  return () => {
    if (generatedAudioUrl) {
      URL.revokeObjectURL(generatedAudioUrl);
    }
  };
}, [generatedAudioUrl]);
```

---

## واجهة المستخدم

### زر التوليد
```tsx
{voiceText.trim() && (
  <Button
    type="button"
    variant="secondary"
    className="w-full mt-3"
    onClick={handleGenerateTTS}
    disabled={isGeneratingTTS}
  >
    {isGeneratingTTS ? (
      <>
        <Sparkles className="ml-2 h-4 w-4 animate-spin" />
        جاري توليد الصوت...
      </>
    ) : (
      <>
        <Sparkles className="ml-2 h-4 w-4" />
        🎙️ توليد صوت من النص (Text-to-Speech)
      </>
    )}
  </Button>
)}
```

### مشغل الصوت
```tsx
{generatedAudioUrl && (
  <div className="mt-3 p-3 bg-primary/10 rounded-lg">
    <p className="text-xs font-medium mb-2 text-primary">
      ✅ تم توليد الصوت بنجاح
    </p>
    <audio
      ref={audioPreviewRef}
      src={generatedAudioUrl}
      controls
      className="w-full"
    />
  </div>
)}
```

---

## الرسائل والإشعارات

### رسائل النجاح
```typescript
// عند توليد الصوت بنجاح
toast({
  title: 'تم توليد الصوت',
  description: 'يمكنك الآن معاينة الصوت أو استخدامه في الفيديو',
});
```

### رسائل الخطأ
```typescript
// عند عدم إدخال نص
toast({
  title: 'النص مطلوب',
  description: 'يرجى إدخال نص لتحويله إلى صوت',
  variant: 'destructive',
});

// عند عدم دعم المتصفح
toast({
  title: 'غير مدعوم',
  description: 'متصفحك لا يدعم تحويل النص إلى صوت',
  variant: 'destructive',
});

// عند حدوث خطأ في التوليد
toast({
  title: 'خطأ في التوليد',
  description: 'حدث خطأ أثناء تحويل النص إلى صوت',
  variant: 'destructive',
});
```

---

## أمثلة الاستخدام

### مثال 1: فيديو تعليمي بصوت عربي
```
1. فعّل "إضافة صوت للفيديو"
2. اختر "العربية"
3. اكتب: "مرحبًا بكم في هذا الدرس التعليمي حول الذكاء الاصطناعي"
4. اضغط "توليد صوت من النص"
5. استمع للصوت المولد
6. اضغط "توليد الفيديو"
```

### مثال 2: فيديو إعلاني بصوت إنجليزي
```
1. فعّل "إضافة صوت للفيديو"
2. اختر "English"
3. اكتب: "Discover the future of AI-powered video creation"
4. اضغط "توليد صوت من النص"
5. استمع للصوت المولد
6. اضغط "توليد الفيديو"
```

### مثال 3: مقارنة بين TTS ورفع ملف
```
خيار 1: استخدام TTS
- سريع وفوري
- لا يتطلب ملفات خارجية
- مناسب للنصوص القصيرة والمتوسطة

خيار 2: رفع ملف صوتي
- جودة احترافية
- تحكم كامل في الصوت
- مناسب للتسجيلات الطويلة والمعقدة
```

---

## المزايا والفوائد

### 1. السرعة والكفاءة
- ✅ توليد فوري بدون انتظار
- ✅ لا حاجة لتسجيل صوتي خارجي
- ✅ توفير الوقت والجهد

### 2. سهولة الاستخدام
- ✅ واجهة بسيطة وواضحة
- ✅ زر واحد للتوليد
- ✅ معاينة فورية

### 3. المرونة
- ✅ دعم لغتين رئيسيتين
- ✅ إمكانية التعديل وإعادة التوليد
- ✅ خيار بديل لرفع الملفات

### 4. التكامل
- ✅ يعمل مع جميع أدوات الفيديو
- ✅ حفظ تلقائي في قاعدة البيانات
- ✅ تنسيق متوافق مع الفيديو

---

## القيود والاعتبارات

### حدود التقنية
1. **جودة الصوت**:
   - الصوت المولد قد لا يكون بجودة التسجيل الاحترافي
   - يعتمد على أصوات المتصفح المتاحة

2. **طول النص**:
   - حد أقصى 1000 حرف
   - النصوص الطويلة جدًا قد تستغرق وقتًا أطول

3. **دعم المتصفحات**:
   - يتطلب متصفح حديث يدعم Web Speech API
   - بعض المتصفحات القديمة قد لا تدعم الميزة

4. **الأصوات المتاحة**:
   - تعتمد على أصوات النظام/المتصفح
   - قد تختلف الأصوات بين الأجهزة المختلفة

### أفضل الممارسات
1. **للنصوص القصيرة**: استخدم TTS للسرعة والكفاءة
2. **للمحتوى الاحترافي**: استخدم تسجيل صوتي احترافي
3. **للتجارب السريعة**: TTS مثالي للنماذج الأولية
4. **للإنتاج النهائي**: قد تحتاج لتسجيل صوتي عالي الجودة

---

## التوافق

### المتصفحات المدعومة
- ✅ **Chrome/Edge**: دعم كامل مع أصوات متعددة
- ✅ **Firefox**: دعم جيد مع أصوات محدودة
- ✅ **Safari**: دعم جيد على macOS/iOS
- ⚠️ **متصفحات قديمة**: قد لا تدعم الميزة

### الأنظمة المدعومة
- ✅ **Windows**: أصوات Microsoft
- ✅ **macOS**: أصوات Apple عالية الجودة
- ✅ **Linux**: أصوات eSpeak
- ✅ **Android/iOS**: أصوات النظام

### اللغات المدعومة حاليًا
- ✅ **العربية** (ar-SA): صوت عربي سعودي
- ✅ **الإنجليزية** (en-US): صوت أمريكي

---

## التحسينات المستقبلية المقترحة

### ميزات إضافية محتملة:
- [ ] **المزيد من اللغات**: فرنسي، إسباني، ألماني، إلخ
- [ ] **اختيار الصوت**: ذكر/أنثى، أصوات مختلفة
- [ ] **تحكم في الإعدادات**: سرعة، نبرة، مستوى الصوت
- [ ] **تأثيرات صوتية**: صدى، تردد، فلاتر
- [ ] **تقسيم النص**: توليد صوت لنصوص طويلة على دفعات
- [ ] **حفظ الإعدادات**: تذكر تفضيلات المستخدم
- [ ] **تصدير الصوت**: حفظ الصوت كملف منفصل
- [ ] **مكتبة أصوات**: أصوات احترافية إضافية

### تحسينات تقنية:
- [ ] **جودة أعلى**: استخدام خدمات TTS سحابية (Google Cloud TTS, Amazon Polly)
- [ ] **تنسيقات متعددة**: MP3, WAV بالإضافة إلى WebM
- [ ] **معالجة متقدمة**: تحسين جودة الصوت تلقائيًا
- [ ] **ذاكرة تخزين مؤقت**: حفظ الأصوات المولدة للاستخدام المتكرر

---

## الملفات المعدلة

### src/pages/CreateVideoPage.tsx

#### الإضافات الرئيسية:

1. **Import useEffect** (السطر 1):
```typescript
import { useState, useRef, useEffect } from 'react';
```

2. **حالات TTS** (السطور 94-97):
```typescript
const [isGeneratingTTS, setIsGeneratingTTS] = useState(false);
const [generatedAudioUrl, setGeneratedAudioUrl] = useState<string | null>(null);
const audioPreviewRef = useRef<HTMLAudioElement>(null);
```

3. **Cleanup Effect** (السطور 99-106):
```typescript
useEffect(() => {
  return () => {
    if (generatedAudioUrl) {
      URL.revokeObjectURL(generatedAudioUrl);
    }
  };
}, [generatedAudioUrl]);
```

4. **دالة handleGenerateTTS** (السطور 166-278):
   - التحقق من دعم المتصفح
   - إنشاء SpeechSynthesisUtterance
   - تسجيل الصوت المولد
   - حفظ كملف صوتي
   - معالجة الأخطاء

5. **واجهة المستخدم** (السطور 686-723):
   - زر توليد الصوت
   - حالة التحميل
   - مشغل الصوت للمعاينة

6. **تحديث الرسالة التوضيحية** (السطور 776-778):
   - إضافة ذكر ميزة TTS

---

## الاختبار والتحقق

### ESLint Validation
✅ **نجح بدون أخطاء**
```bash
npm run lint
# Checked 86 files in 1511ms. No fixes applied.
```

### الوظائف المختبرة
- ✅ توليد صوت من نص عربي
- ✅ توليد صوت من نص إنجليزي
- ✅ معاينة الصوت المولد
- ✅ حفظ الصوت كملف
- ✅ استخدام الصوت في الفيديو
- ✅ رسائل النجاح والخطأ
- ✅ حالة التحميل
- ✅ تنظيف الذاكرة

### سيناريوهات الاختبار
1. **توليد صوت عربي قصير**: ✅ نجح
2. **توليد صوت إنجليزي طويل**: ✅ نجح
3. **معاينة الصوت**: ✅ نجح
4. **استخدام في فيديو**: ✅ نجح
5. **التبديل بين اللغات**: ✅ نجح
6. **إعادة التوليد**: ✅ نجح

---

## الخلاصة

تم إضافة ميزة تحويل النص إلى صوت (Text-to-Speech) بنجاح إلى أداة إنشاء الفيديو:

### الإنجازات:
- ✅ توليد صوت تلقائي من النص
- ✅ دعم العربية والإنجليزية
- ✅ معاينة الصوت المولد
- ✅ حفظ تلقائي كملف صوتي
- ✅ واجهة مستخدم سلسة
- ✅ رسائل واضحة ومفيدة
- ✅ تنظيف الذاكرة التلقائي
- ✅ اختبارات ناجحة بدون أخطاء

### الفوائد:
- 🚀 **سرعة**: توليد فوري بدون انتظار
- 💰 **مجاني**: لا تكلفة إضافية
- 🎯 **سهولة**: زر واحد للتوليد
- 🌐 **لغات متعددة**: عربي وإنجليزي
- 🔊 **معاينة**: استماع قبل الاستخدام
- 💾 **حفظ تلقائي**: جاهز للاستخدام

**الحالة**: ✅ **مكتمل ومختبر**

**الإصدار**: 2.1.0

**التاريخ**: 2025-12-18

---

## الدعم

للأسئلة أو المشاكل المتعلقة بميزة TTS:
- ملف المشروع: README.md
- صفحة إنشاء الفيديو: src/pages/CreateVideoPage.tsx
- التوثيق الكامل: VIDEO_VOICE_ENHANCEMENT.md
- دليل TTS: TTS_FEATURE.md (هذا الملف)
