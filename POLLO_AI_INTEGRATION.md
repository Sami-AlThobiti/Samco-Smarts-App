# 🎬 Pollo AI Video Effects Integration

## نظرة عامة

تم إضافة ميزة **تأثيرات الفيديو** إلى تطبيق سامكو باستخدام تقنية **Pollo AI**. هذه الميزة تتيح للمستخدمين إنشاء فيديوهات احترافية باستخدام قوالب جاهزة ومؤثرات متقدمة.

## المميزات

✅ **15 تأثير فيديو احترافي**
✅ **3 فئات منظمة**: تفاعلي، فيرال، فني
✅ **واجهة سهلة الاستخدام** مع تبويبات
✅ **رفع صور متعدد** (صورة أو صورتين حسب التأثير)
✅ **إعدادات قابلة للتخصيص** (نسبة العرض، المدة)
✅ **تصفية محتوى آمن** (بدون محتوى للبالغين)

## التأثيرات المتاحة

### 🤗 تفاعلي (Interaction)

| التأثير | الوصف | الصور المطلوبة | الأيقونة |
|---------|-------|----------------|----------|
| **عناق** | فيديو عناق رومانسي بين صورتين | 2 | 🤗 |
| **قبلة** | فيديو قبلة رومانسية بين صورتين | 2 | 💋 |
| **موعد وهمي** | فيديو موعد رومانسي بين صورتين | 2 | 💑 |

### 🔥 فيرال (Viral)

| التأثير | الوصف | الصور المطلوبة | الأيقونة |
|---------|-------|----------------|----------|
| **دوران 360°** | دوران كامل 360 درجة حول الصورة | 1 | 🔄 |
| **تكبير الأرض** | تكبير سينمائي من الأرض إلى الصورة | 1 | 🌍 |
| **داخل الفم** | حركة الكاميرا داخل الفم بشكل كوميدي | 1 | 👄 |
| **الدخول إلى المشهد** | انتقال سينمائي حيث تتحرك الكاميرا للأمام داخل الصورة | 1 | 🌀 |
| **عمق ثلاثي الأبعاد** | تأثير عمق ثلاثي الأبعاد واقعي من صورة واحدة | 1 | 📐 |
| **كشف بطولي** | كشف بطولي مع إضاءة درامية وحركة كاميرا | 1 | 🦸 |

### 🎨 فني (Art)

| التأثير | الوصف | الصور المطلوبة | الأيقونة |
|---------|-------|----------------|----------|
| **شخصية أكشن** | تحويل الصورة إلى شخصية أكشن 3D | 1 | 🦸 |
| **عضلات** | إضافة عضلات قوية بشكل كوميدي | 1 | 💪 |
| **شخصية أكشن ثلاثية الأبعاد** | تحويل الشخص إلى شخصية أكشن واقعية عالية الجودة | 1 | 🧸 |
| **حركة شعر وملابس** | حركة واقعية للشعر والملابس مع الرياح | 1 | 🌬️ |
| **إضاءة سينمائية متحركة** | إضاءة سينمائية تتحرك عبر وجه وجسم الشخص | 1 | ✨ |
| **إحياء الشخصية** | إحياء الشخصية بحركات وجه وتنفس طبيعي | 1 | 👁️ |

## البنية التقنية

### الملفات المضافة

```
src/
├── types/
│   └── effects.ts                    # أنواع TypeScript للتأثيرات
├── data/
│   └── effects.json                  # قاعدة بيانات التأثيرات
├── pages/
│   ├── VideoEffectsPage.tsx          # صفحة عرض التأثيرات
│   └── EffectDetailPage.tsx          # صفحة تفاصيل التأثير
└── routes.tsx                        # المسارات المحدثة
```

### الملفات المعدلة

```
src/
├── pages/
│   └── HomePage.tsx                  # إضافة بطاقة تأثيرات الفيديو
├── components/ui/
│   └── AIBackground.tsx              # إضافة variants جديدة
└── routes.tsx                        # إضافة مسارات جديدة
```

## هيكل البيانات

### EffectTemplate

```typescript
interface EffectTemplate {
  id: string;                         // معرف فريد
  title_ar: string;                   // العنوان بالعربية
  title_en: string;                   // العنوان بالإنجليزية
  description_ar: string;             // الوصف بالعربية
  description_en: string;             // الوصف بالإنجليزية
  category: EffectCategory;           // الفئة
  icon: string;                       // الأيقونة (emoji)
  previewImage: string;               // صورة المعاينة
  inputs: {
    images: 1 | 2;                    // عدد الصور المطلوبة
    promptOptional?: boolean;         // هل البرومبت اختياري
  };
  defaults: {
    aspect: AspectRatio;              // نسبة العرض الافتراضية
    durationSec: Duration;            // المدة الافتراضية
  };
  engine: {
    provider: 'pollo_api' | 'internal'; // مزود الخدمة
    model: string;                    // اسم الموديل
  };
  isAdult?: boolean;                  // محتوى للبالغين (مفلتر)
}
```

### مثال من effects.json

```json
{
  "id": "effect_hug",
  "title_ar": "عناق",
  "title_en": "Hug",
  "description_ar": "أنشئ فيديو عناق رومانسي بين صورتين",
  "description_en": "Create a romantic hug video between two images",
  "category": "interaction",
  "icon": "🤗",
  "previewImage": "https://...",
  "inputs": {
    "images": 2,
    "promptOptional": false
  },
  "defaults": {
    "aspect": "9:16",
    "durationSec": 8
  },
  "engine": {
    "provider": "pollo_api",
    "model": "interaction_hug"
  }
}
```

## تدفق العمل (Workflow)

### 1. الصفحة الرئيسية (HomePage)

```
المستخدم يرى 3 بطاقات:
├── 🎨 إنشاء صورة AI
├── 🎬 إنشاء فيديو AI
└── ✨ تأثيرات الفيديو ← جديد
```

### 2. صفحة التأثيرات (VideoEffectsPage)

```
المستخدم يختار فئة:
├── 🤗 تفاعلي (3 تأثيرات)
├── 🔥 فيرال (3 تأثيرات)
└── 🎨 فني (2 تأثيرات)

ثم يضغط على بطاقة التأثير
```

### 3. صفحة التفاصيل (EffectDetailPage)

```
المستخدم:
1. يرفع الصور المطلوبة (1 أو 2)
2. يختار الإعدادات:
   - نسبة العرض (9:16, 16:9, 1:1)
   - المدة (5s, 8s, 10s)
3. يضغط "توليد الفيديو"
4. ينتظر النتيجة (30 ثانية - دقيقتين)
5. يحمل أو يشارك الفيديو
```

## تكامل Pollo AI API

### الحالة الحالية

⚠️ **ملاحظة**: التكامل الحالي يستخدم **محاكاة (Mock)** لتوليد الفيديو. يجب استبداله بـ Pollo AI API الفعلي.

### خطوات التكامل الفعلي

#### 1. الحصول على API Key

```bash
# زيارة Pollo AI Platform
https://pollo.ai/api-platform

# التسجيل والحصول على API Key
# إضافة المفتاح إلى .env
VITE_POLLO_API_KEY=your_api_key_here
```

#### 2. قراءة الوثائق

```
الوثائق الرسمية:
https://docs.pollo.ai/

أقسام مهمة:
- Authentication
- Video Effects API
- Job Status Polling
- Error Handling
```

#### 3. تحديث src/services/api.ts

```typescript
// إضافة دالة Pollo AI
export async function generatePolloEffect(request: EffectGenerationRequest): Promise<EffectGenerationResponse> {
  const POLLO_API_KEY = import.meta.env.VITE_POLLO_API_KEY;
  
  // 1. رفع الصور
  const imageUrls = await uploadImagesToPollo(request.images);
  
  // 2. إنشاء Job
  const response = await fetch('https://api.pollo.ai/v1/effects/create', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${POLLO_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      effect_type: request.effectId,
      images: imageUrls,
      aspect_ratio: request.aspect,
      duration: request.duration,
    }),
  });
  
  const data = await response.json();
  
  // 3. إرجاع Job ID
  return {
    jobId: data.job_id,
    status: 'pending',
  };
}

// دالة التحقق من حالة Job
export async function checkPolloJobStatus(jobId: string): Promise<EffectGenerationResponse> {
  const POLLO_API_KEY = import.meta.env.VITE_POLLO_API_KEY;
  
  const response = await fetch(`https://api.pollo.ai/v1/jobs/${jobId}`, {
    headers: {
      'Authorization': `Bearer ${POLLO_API_KEY}`,
    },
  });
  
  const data = await response.json();
  
  return {
    jobId: jobId,
    status: data.status,
    videoUrl: data.video_url,
    error: data.error,
  };
}
```

#### 4. تحديث EffectDetailPage.tsx

```typescript
const handleGenerate = async () => {
  setIsGenerating(true);

  try {
    // إنشاء Job
    const result = await generatePolloEffect({
      effectId: effect.id,
      images: images,
      aspect: aspect,
      duration: duration,
    });

    // Polling للتحقق من الحالة
    const checkStatus = async () => {
      const status = await checkPolloJobStatus(result.jobId);
      
      if (status.status === 'completed') {
        setGeneratedVideoUrl(status.videoUrl);
        toast({
          title: '✅ تم التوليد',
          description: 'تم إنشاء الفيديو بنجاح',
        });
      } else if (status.status === 'failed') {
        throw new Error(status.error || 'فشل التوليد');
      } else {
        // انتظر 5 ثوانٍ ثم حاول مرة أخرى
        setTimeout(checkStatus, 5000);
      }
    };

    await checkStatus();
  } catch (error) {
    toast({
      title: 'خطأ',
      description: 'فشل توليد الفيديو. حاول مرة أخرى.',
      variant: 'destructive',
    });
  } finally {
    setIsGenerating(false);
  }
};
```

## تصفية المحتوى (Content Safety)

### التأثيرات المفلترة

تم **استبعاد** التأثيرات التالية من Pollo AI لأنها غير مناسبة:

❌ Bikini
❌ Jiggle
❌ Twerk
❌ أي محتوى جنسي أو غير لائق

### التأثيرات المسموحة

✅ جميع التأثيرات في `effects.json` آمنة للاستخدام العام
✅ مناسبة لجميع الأعمار
✅ محتوى عائلي

### إضافة تأثيرات جديدة

عند إضافة تأثير جديد، تأكد من:

1. **مراجعة المحتوى**: هل التأثير مناسب؟
2. **إضافة `isAdult: true`** إذا كان للبالغين فقط
3. **فلترة في الكود**:

```typescript
// في VideoEffectsPage.tsx
const safeEffects = effects.filter(e => !e.isAdult);
```

## الإعدادات المتاحة

### نسب العرض (Aspect Ratios)

| النسبة | الاستخدام | الأبعاد |
|--------|-----------|---------|
| **9:16** | TikTok, Reels, Stories | 1080×1920 |
| **16:9** | YouTube, TV | 1920×1080 |
| **1:1** | Instagram Post | 1080×1080 |

### المدة (Duration)

| المدة | الاستخدام |
|------|-----------|
| **5 ثوانٍ** | تأثيرات سريعة، Loops |
| **8 ثوانٍ** | الافتراضي، متوازن |
| **10 ثوانٍ** | محتوى أطول، قصص |

## واجهة المستخدم

### الألوان والأيقونات

```css
/* بطاقة تأثيرات الفيديو */
.video-effects-card {
  border-color: var(--accent);
  background: gradient(accent → primary);
  icon: ✨
}

/* التبويبات */
.tab-interaction { icon: 🤗 }
.tab-viral { icon: 🔥 }
.tab-art { icon: 🎨 }
```

### الرسائل

```typescript
// رسائل النجاح
"✅ تم التوليد - تم إنشاء الفيديو بنجاح"

// رسائل الخطأ
"❌ خطأ - فشل توليد الفيديو. حاول مرة أخرى."

// رسائل التحذير
"⚠️ يرجى رفع صورة واحدة"
"⚠️ يرجى رفع صورتين"

// رسائل المعلومات
"💡 النتائج قد تستغرق من 30 ثانية إلى دقيقتين"
```

## الاختبار

### اختبار يدوي

1. **الصفحة الرئيسية**
   - [ ] بطاقة "تأثيرات الفيديو" تظهر
   - [ ] الضغط عليها ينقل إلى /video-effects

2. **صفحة التأثيرات**
   - [ ] التبويبات تعمل (تفاعلي، فيرال، فني)
   - [ ] جميع التأثيرات تظهر في الفئة الصحيحة
   - [ ] الضغط على تأثير ينقل إلى صفحة التفاصيل

3. **صفحة التفاصيل**
   - [ ] رفع الصور يعمل (Drag & Drop + Click)
   - [ ] حذف الصور يعمل
   - [ ] اختيار الإعدادات يعمل
   - [ ] زر "توليد الفيديو" يعمل
   - [ ] عرض النتيجة يعمل
   - [ ] تحميل الفيديو يعمل
   - [ ] مشاركة الفيديو يعمل
   - [ ] إعادة التوليد يعمل

### اختبار تلقائي

```typescript
// TODO: إضافة اختبارات Unit Tests
describe('VideoEffectsPage', () => {
  it('should render all effect categories', () => {
    // ...
  });
  
  it('should filter effects by category', () => {
    // ...
  });
});

describe('EffectDetailPage', () => {
  it('should upload images correctly', () => {
    // ...
  });
  
  it('should generate video with correct parameters', () => {
    // ...
  });
});
```

## الأداء

### تحسينات مطلوبة

1. **Lazy Loading للصور**
   ```typescript
   <img loading="lazy" src={effect.previewImage} />
   ```

2. **Caching للتأثيرات**
   ```typescript
   const cachedEffects = useMemo(() => effects, []);
   ```

3. **Compression للصور المرفوعة**
   ```typescript
   const compressImage = async (file: File) => {
     // استخدام مكتبة مثل browser-image-compression
   };
   ```

## الأمان

### حماية API Key

```typescript
// ❌ خطأ - لا تضع API Key في Frontend
const API_KEY = 'sk-1234567890';

// ✅ صحيح - استخدم Backend Proxy
const response = await fetch('/api/pollo/generate', {
  method: 'POST',
  body: JSON.stringify(request),
});
```

### التحقق من الصور

```typescript
// التحقق من نوع الملف
const allowedTypes = ['image/png', 'image/jpeg', 'image/webp'];
if (!allowedTypes.includes(file.type)) {
  throw new Error('نوع الملف غير مدعوم');
}

// التحقق من حجم الملف (مثلاً 10MB)
const maxSize = 10 * 1024 * 1024;
if (file.size > maxSize) {
  throw new Error('حجم الملف كبير جدًا');
}
```

## الروابط المفيدة

### Pollo AI

- **الموقع الرسمي**: https://pollo.ai/
- **Video Effects**: https://pollo.ai/video-effects
- **API Platform**: https://pollo.ai/api-platform
- **الوثائق**: https://docs.pollo.ai/

### مراجع تقنية

- **React Dropzone**: https://react-dropzone.js.org/
- **TypeScript**: https://www.typescriptlang.org/
- **Tailwind CSS**: https://tailwindcss.com/

## الخطوات التالية

### المرحلة 1: التكامل الأساسي ✅

- [x] إنشاء هيكل الملفات
- [x] تصميم واجهة المستخدم
- [x] إضافة التأثيرات الأساسية
- [x] تصفية المحتوى

### المرحلة 2: تكامل Pollo AI API ⏳

- [ ] الحصول على API Key
- [ ] قراءة الوثائق
- [ ] تنفيذ دوال API
- [ ] اختبار التكامل

### المرحلة 3: التحسينات 📋

- [ ] إضافة المزيد من التأثيرات
- [ ] تحسين الأداء
- [ ] إضافة اختبارات تلقائية
- [ ] تحسين معالجة الأخطاء

### المرحلة 4: الميزات المتقدمة 🚀

- [ ] معاينة مباشرة للتأثيرات
- [ ] حفظ التأثيرات المفضلة
- [ ] مشاركة على وسائل التواصل
- [ ] تاريخ التأثيرات المستخدمة

## الخلاصة

تم إضافة ميزة **تأثيرات الفيديو** بنجاح إلى تطبيق سامكو مع:

✅ **15 تأثير احترافي** منظم في 3 فئات
✅ **واجهة مستخدم سهلة** مع تبويبات وبطاقات
✅ **تصفية محتوى آمن** بدون محتوى غير لائق
✅ **بنية قابلة للتوسع** لإضافة المزيد من التأثيرات
✅ **جاهز للتكامل** مع Pollo AI API

**الحالة**: ✅ جاهز للاستخدام (مع محاكاة)
**التاريخ**: 2025-12-18
**الإصدار**: 4.1.0 (Pollo AI Video Effects - Extended)

---

**ملاحظة**: لتفعيل التكامل الكامل مع Pollo AI، يجب الحصول على API Key واتباع خطوات التكامل المذكورة أعلاه.
